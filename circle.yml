#
# Timedance CircleCI Configuration
#

machine:
  environment:
    TAG: $(sed 's/master/latest/;s/\//\-/' <<<$CIRCLE_BRANCH)

dependencies:
  pre:
    - go get -v github.com/spf13/hugo

test:
  override:
    - echo "test"

deployment:
  prod:
    branch: master
    commands:
      - hugo
      - sh deploy.sh www.timedancemusic.com
